import numpy as np

# arr = [['a', 'b', 'c', 'd'],
#        [1, 1, 1, 1],
#        [1, 2, 3, 0],
#        [3, 3, 3, 3]]
# attr = arr[0].copy()

# arr = np.transpose(arr)
# arr = np.delete(arr, 0, 1)

# value = {}
# j = 0
# for i in arr:
#     print(np.unique(i))
#     value[attr[j]] = np.unique(i)
#     j = j + 1

# print(value['a'][1])

            # print(key)
            # print(test_data.iloc[i, data_attr.index(key)]) # 'low'
            
            # 해당 attribute가 없는 경우 다음 attr을 비교하기
            # if temp_dt[key].get(test_data.iloc[i, data_attr.index(key)]) == None:
            #     temp_dt = list(temp_dt.values())[0]

            #     # 다음에 dictionary이지만 마지막이 아닌 경우
            #     if (type(temp_dt) == type({})):
            #         if (list(temp_dt.values())[0] != type('')):
            #             temp_dt = list(temp_dt.values())[0]

            # else:
            #     temp_dt = temp_dt[key].get(test_data.iloc[i, data_attr.index(key)])

            # print(temp_dt)

a = np.sum([0.0 ,0.0])
print(a == 0)
print(type(a))